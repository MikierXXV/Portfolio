import React, {Component} from "react";

export default class Skills extends Component {
    render() {
        return(
            <section id="skills">
                <div>You</div>
            </section>
        );
    }
}